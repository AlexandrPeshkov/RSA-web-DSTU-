@model BestStepView
    ViewData["Title"] = "Конфигурация";
    Layout = "_Layout";
}

@using (Html.BeginForm())
{
    <h1>@Html.DisplayNameFor(Model => Model)</h1>

    @Html.RenderPartial("_Direction", Model.Direction)

}
